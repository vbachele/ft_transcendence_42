import React from 'react'

// const UseFetchID = (url:string) => {
	
// 	const data = require('./data.json');
// 	const user = data.users.find(user => user.id === 1);
	
// 	return (
//   )
// }

// export default UseFetchID